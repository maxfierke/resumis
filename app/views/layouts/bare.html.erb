<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render :partial => 'shared/head' %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <div class="container">
      <%= bootstrap_flash %>
      <%= yield %>
    </div>

    <footer class="footer-page">
      <div class="container">
        <p>
          Powered by Resumis
          <% if user_signed_in? %>
            &middot;
            <%= link_to "Logout", destroy_user_session_url(host: request.domain), :method => :delete %>
          <% end %>
        </p>
      </div>
    </footer>
  </body>
</html>
