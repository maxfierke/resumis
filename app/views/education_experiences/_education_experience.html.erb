<section id="educationexp_<%= education_experience.id %>"
         class="panel panel-default panel-education-experience
                <% if education_experience.end_date %>
                  current-education-experience
                <% else %>
                  past-education-experience
                <% end %>"
  >
  <div class="panel-heading">
    <h4 class="panel-title clearfix">
      <span class="pull-left">
        <%= education_experience.diploma %><br />
        <small><%= education_experience.school_name %></small>
      </span>
      <span class="pull-right">
        <%= education_experience.date_range %>

        <% if user_signed_in? and
              education_experience.user_id == current_user.id and
              !(controller_name == 'resumes' and action_name == 'show')
        %>
          <%= link_to edit_manage_education_experience_path(education_experience), :class => 'btn btn-xs', :title => "#{ t('.edit', :default => t('helpers.links.edit')) }" do %>
            <%= icon 'pencil' %>
          <%- end -%>
          <%= link_to manage_education_experience_path(education_experience), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= icon 'remove' %>
          <%- end -%>
        <% end %>
      </span>
    </h4>
  </div>
  <% if education_experience.description.length > 0 %>
  <div class="panel-body">
    <div class="row">
      <div class="col-xs-12">
        <p><%= markdown(education_experience.description) %></p>
      </div>
    </div>
  </div>
  <% end %>
</section>
